server:
  port: 8884
  servlet:
    context-path: /pandama/activiti

spring:
  application:
    name: pandama-activiti
  profiles:
    # 默认激活test环境，被命令行参数覆盖；这里写出来主要方便本地调试用（本地调试无需指定命令行参数了）
    active: dev